# FROZEN before any schedule comparison. Master v4 §3.8, flaw E6.
#
# If these move between runs you are comparing tuning effort, not schedules.
# Tune on sequence 07, report on 08 -- never tune and report on the same one.
#
# Every value here is read at startup. Nothing in src/ may hardcode a threshold
# inline; if you find one, that is a bug, not a shortcut.

frozen: true
frozen_on: null        # set to the commit sha at the Day-0 freeze
tuning_sequence: "07"
reporting_sequence: "08"

# --- traversability bitfield (math §7.1), 0 = traversable -------------------
traversability:
  h_vehicle_m: 1.8         # bit 0  clearance
  theta_max_deg: 20.0      # bit 1  slope, compared as tan(theta_max)
  s_max_m: 0.15            # bit 2  step, max |z_c - z_nbr| over 4-neighbours
  baseline_m: 0.50         # bits 1 and 2 are differenced over THIS distance,
                           # not over one cell. Without it the same 12 cm kerb
                           # is a gradient of 1.200 at 5 cm and 0.240 at 25 cm
                           # against one frozen theta_max, so a feature is a
                           # wall on the fine rings and flat ground on the
                           # coarse ones and on M* -- the eq. (23) asymmetry.
                           # Bounded by the scene, not by taste: it must be
                           # > 0.12/tan(20 deg) = 0.33 m for the 12 cm kerb to
                           # read passable at EVERY cell size, and
                           # < 0.40/tan(20 deg) = 1.10 m for the 40 cm pothole
                           # rim to still fail. 0.50 m is the middle of that
  depression_baseline_m: 4.0    # bit 6 (R10). A SECOND baseline alongside the
                                # 0.50 m one above, not a replacement. 0.50 m is
                                # tuned so a 12 cm kerb stays passable at every
                                # cell size, which is exactly why it cannot see a
                                # 2 m wide feature: the stencil is shorter than
                                # the hazard. 4.0 m straddles a 2 m depression
                                # with a full cell of rim on each side.
  depression_dip_min_m: 0.15    # bit 6 threshold, as a DEPTH below the chord.
                                # Set equal to s_max_m deliberately: a 15 cm dip
                                # is as untraversable as a 15 cm step, so one
                                # number moves rather than two.
  vru_decay_frames: 5           # R5. Frames since a VRU was OBSERVED IN THIS
                                # CELL before FLAG_VRU_SEEN clears -- not frames
                                # since the cell was seen, which is the ambiguity
                                # the design doc flags as the failure mode.
                                # [!] FIRST GUESS, 2026-09-23. Nobody has tuned
                                # this. 5 is half a second at 10 Hz, long enough
                                # to outlast a VRU being briefly occluded and
                                # short enough not to latch a whole street.
                                # Hard ceiling is 7: the counter is 3 bits.
                                # Needs measurement against a real sequence.
                           # window. A ring coarser than baseline_m/2 falls
                           # back to one cell, so this is a no-op at 40 cm and
                           # 80 cm and at the 25 cm planning lattice.
  sigma2_max_m2: 0.0025    # bit 3  roughness, (5 cm)^2
  n_min: 3                 # bit 5  confidence -- below this, fail safe
  drivable_classes: [road, parking, sidewalk, other-ground, terrain]  # bit 4

# --- named features (math §7.4) ---------------------------------------------
# The problem statement names curbs and potholes as the reason a 2D grid is not
# enough. These are FEATURES, not traversability bits: §7.1 says whether the
# vehicle can be somewhere, this says what is there. Reported alongside the
# bitfield and never folded into it -- a 12 cm kerb is passable and is still a
# kerb, and a planner wants both facts.
features:
  min_obs: 1               # NOT traversability.n_min. n_min = 3 asks "may the
                           # vehicle drive here"; this asks "is this height a
                           # measurement or the default 0". One return answers
                           # that, and it is where bitfield()'s own `geometric`
                           # mask draws the line. Set to 3 and the detectors go
                           # silent on a sparse map -- 1.2% of ring 0 survives
                           # a five-cell stencil at n_min, and none of it is a
                           # kerb. Sparse-cell noise is rejected by the run
                           # test and the rim pairs, not by this.
  curb:
    min_height_m: 0.05     # below this is road texture, not a kerb
    max_height_m: 0.25     # above this is a wall or a vehicle, not a kerb
    baseline_m: 0.20       # SHORT on purpose. §7.1's 0.50 m baseline smears a
                           # kerb into a gentle slope, which is the right
                           # answer for "can I climb it" and the wrong one for
                           # "where is the edge".
                           #
                           # ⚑ TRIED 0.30 AND REVERTED, 2 Sep. Chosen from
                           #   three sequences, where it raised the ring-0
                           #   median (07 8.5->9.1, 08 8.6->9.8, 05 9.1->9.5).
                           #   Across all ELEVEN it raises the median 0.7 cm
                           #   and nearly TRIPLES the cross-sequence spread:
                           #
                           #       0.20 m   median 8.2   range 8.1-9.1   sd 0.36
                           #       0.30 m   median 8.9   range 7.3-10.2  sd 0.89
                           #
                           #   The tight band is the claim -- eleven
                           #   independent recordings, different dates and
                           #   calibrations, agreeing to 1.0 cm. Trading it for
                           #   0.7 cm of median is trading the result for the
                           #   number. Three sequences were not enough to pick
                           #   this and eleven were.
                           #
                           # ⚑ It also only ever moved ring 0. `baseline_k` is
                           #   an integer and `round(0.30/(2*0.10))` is
                           #   round(1.4999999999999998) = 1, so at 10 cm cells
                           #   and coarser the span stays 2*cell_m whatever
                           #   this says. Below baseline_m/2 the knob does
                           #   nothing, which is worth knowing before tuning it.
    min_run_cells: 3       # a kerb is a line; a lone raised cell is a rock.
                           # 3 = the cell plus its two perpendicular
                           # neighbours, which is what detect_curbs tests.
  pothole:
    min_depth_m: 0.08      # under 8 cm is a patch or a seam
    max_depth_m: 0.50      # over 50 cm it is not a pothole. Bounded above for
                           # the same reason the curb band is: on seq 08 ring 2
                           # this found 156 "potholes" at a median 71.5 cm and
                           # a p90 of 200 cm -- ditches, kerb-line drop-offs
                           # and the space under parked cars. Those are real
                           # hazards and they are a different detector.
    rim_baseline_m: 0.75   # the surface is measured on a ring this far out --
                           # wider than the 0.60 m pothole of §1.4 so the rim
                           # samples land on intact road
    min_rim_pairs: 2       # of 4 OPPOSED rim axes. Pairs, not samples: a rim
                           # seen on one side only is a slope reading, not a
                           # surface, and it turns every downgrade at the
                           # window edge into a field of potholes.
    max_depressed_rim: 0.5 # if more than half the rim is down too this is a
                           # dip, a ditch or a downgrade -- not a pothole
    max_rim_spread_m: 0.08 # the rim must agree with itself. Half a rim on the
                           # road and half on a 12 cm kerb reads the whole
                           # carriageway as a 10 cm pothole; if the surface
                           # spans more than this there is no single surface
                           # for a hole to be in.

# --- occupancy ---------------------------------------------------------------
occupancy:
  # Unknown is decided by observation count, NOT by log-odds near zero.
  unknown_below_obs: 1
  log_odds_hit: 4
  log_odds_miss: -2
  log_odds_clamp: [-64, 63]
  log_odds_occupied: 0     # l_occ of §10.1. The section names it in the state
                           # rule and never gives it a value; this file never
                           # had one either, so occupancy_state() had nothing
                           # to compare against. 0 = "more hits than misses",
                           # the neutral reading. Frozen with the rest.

# --- fusion (math §3.3) ------------------------------------------------------
fusion:
  process_noise_m2_per_s: 1.0e-4   # q. Without it sigma^2 collapses toward
                                   # zero, K goes to 0 and the cell stops
                                   # responding to new evidence -- the classic
                                   # filter-lock failure. §3.3.
  frame_dt_s: 0.1                  # 10 Hz

# --- split / merge variance model (math §5.2 eq. 17) ------------------------
# sigma2_child = sigma2_parent + kappa ||grad z||^2 (c_p^2 - c_c^2) + alpha
split_merge:
  kappa: 0.0625            # 1/16, §5.2 as written.
                           # ⚑ §5.2 derives this from "d^2 = c_p^2/16", but
                           # (17) multiplies it by (c_p^2 - c_c^2), not c_p^2.
                           # The stated geometry actually gives 1/12, at every
                           # refinement ratio m. 1/16 under-inflates by 25%.
                           # Kept as documented; the room decides, not me.
                           # Pinned in tests/test_splitmerge.py.
  alpha_m2: 0.0            # sub-cell roughness. UNCALIBRATED: §5.2 calibrates
                           # it against the reference map (§9), which is
                           # blocked on the download. 0 is also the only value
                           # consistent with §5.3's "splitting flat ground
                           # costs nothing" and §5.4's unit test (c) -- raising
                           # alpha means restating both, in the same commit.

# --- sensor (HDL-64E on KITTI) ----------------------------------------------
sensor:
  height_m: 1.73
  phi_min_deg: -24.8
  blind_cone_m: 3.74       # h / tan|phi_min|. Unknown, never free. 11% of Ring 0.
  # Range-image geometry (num_rings/num_azimuth/phi_max and the derived bin
  # widths) lives in configs/frnet.yaml, which is what range_image.py reads.
  # The old d_theta_deg / d_phi_deg keys here were never read by anything.
  # Measurement-noise model, math §3.2 eq (12). sigma_phi is stated in the
  # math doc; sigma_r is NOT -- 2 cm is the HDL-64E datasheet figure and an
  # assumption of ours. It only moves the near-field floor: the 8.7 cm at 50 m
  # and 17.5 cm at 100 m figures are dominated by the sigma_phi term.
  sigma_r_m: 0.02
  sigma_phi_deg: 0.1

# --- scatter (math §3.4) -----------------------------------------------------
scatter:
  # "sorted" reduces segments of a sorted index array: scratch is sized by
  # POINTS (~1.8 MB) and needs no per-frame clear. "atomic" is the literal
  # integer-atomics path: a dense per-cell accumulator, ~11.9 MB at 745,000
  # cells, cleared every frame. Both are bit-identical; this is a cost choice.
  mode: sorted
  max_points_per_frame: 150000
  weight_scale: 1024

# --- plan regret (math §8) ---------------------------------------------------
# w of eq. (23)'s cost functional, J = sum over path cells of w(c)*dl.
# §8.1 says "w derived from the traversability bitfield" and stops there, so
# the derivation lives in src/eval/plan_regret.py and the numbers live here.
plan:
  cell_m: 0.25             # the PLANNER's resolution, not the map's. A vehicle
                           # plans in vehicle-sized steps; the map is finer
                           # than this wherever it matters, which is the point.
  w_base: 1.0
  w_roughness: 2.0         # drivable and undesirable -- a weight, not a wall
  w_class: 3.0             # ditto: geometry decides, semantics filters (§7.1)
  w_unknown: 4.0           # ⚑ unknown is PASSABLE here, unlike everywhere else
                           # in this project. At P_fill < 2% per frame (§1.3)
                           # most of the far field is unobserved, so with
                           # unknown impassable no path exists and R(S) is
                           # undefined for every schedule. Read the regret
                           # together with PlanResult.unknown_fraction: zero
                           # regret along a mostly-unknown path means the
                           # sequence was too short, not that coarsening was free.
  corridor_tau: 5.0        # tau of eq. (25), in cost units. Sets the refinement
                           # budget and maps onto the pool size.

# --- visibility cleanup (math §10.4) ----------------------------------------
visibility:
  # Never clear a cell that has a return in the current scan. Without this
  # guard the cleanup eats fences, poles and sign posts within a few frames.
  protect_current_returns: true
  range_tolerance_m: 0.30
  frames_to_clear: 3
  # Cap on candidate cells per frame -- the currently-OCCUPIED cells, since a
  # free cell has nothing to clear. It sizes the cleanup's preallocated
  # scratch at 64 B per cell against a float32 range image, exactly the way
  # `scatter.max_points_per_frame` sizes the scatter scratch.
  #
  # MEASURED 2 Sep, and the answer was "do not put a number here".
  #   scripts/measure_visibility_cap.py, real iter_pipeline -> MapEngine.step,
  #   schedule 5/10/20/40, peak occupied cells offered to the cleanup:
  #
  #       seq 07, all 1,101 frames    max 314,442   (median 187,921)
  #       seq 08, all 4,071 frames    max 455,714   (median 225,629)
  #       seq 00, all 4,541 frames    max 278,226   (median 186,183)
  #
  #   The old 150,000 was an argument, not a measurement, and it was wrong by
  #   a factor of three: it dropped 52.3% of 07's peak and 67.1% of 08's,
  #   untested, in silence.
  #
  # ⚑ THE PEAK IS UNPREDICTABLE, not merely large. It does NOT scale with
  #   sequence length -- 00 is the LONGEST of the three and has the LOWEST
  #   peak. An earlier version of this note claimed it scaled, from two
  #   points; the third refutes it. The driver is scene density (08 is a
  #   dense urban loop, 00 is more open), which nothing here bounds a priori
  #   for the 19 sequences still unmeasured. A 1.64x spread with no predictor
  #   is why a fitted number is a bet, and losing it is silent: dropped cells
  #   keep their occupancy, are never tested, and cannot appear in `cleared`.
  #
  #   `null` means "the grid's own slot count", resolved per schedule at
  #   allocation time by `gpu.allocators.resolve_candidate_cap`. The occupied
  #   set is bounded by construction -- a cell must exist to be occupied -- so
  #   this makes truncation IMPOSSIBLE rather than unlikely, and turns the
  #   bound back into the compile-time guarantee the memory claim is supposed
  #   to be. 910,000 slots and 58.24 MB of scratch at 5/10/20/40; 570,000 and
  #   36.48 MB at 5/10/50. Working memory, not map memory, so the cell-count
  #   ratios in the report are unaffected either way.
  #
  #   An explicit integer still works, for a deliberately tighter bound on a
  #   constrained target. `StepCounters.truncated` now counts whatever such a
  #   cap drops and `vrgrid.run` prints it, so that trade is visible.
  max_candidate_cells: null

# --- refinement pool (master v4 §3.4) ---------------------------------------
refinement_pool:
  blocks: 512
  cells_per_block: 16      # 512 * 16 * 12 B = 98 KB, fixed
  evict_by: [closeness, dynamism, time_to_collision]
