# Open items — the master list

> **VERIFICATION STANDING NOTE — read before trusting any status line below.**
> **Last full verification: 2026-09-23** (`reports/full-project-audit-2026-09-23.md`).
> Statuses in this file have been wrong in both directions: items marked closed that
> were not (R-b, R-d, S-6), items closed for days while still listed open (D2), items
> closed with an incomplete artifact (R-a), and items that existed nowhere at all
> (R10). Three separate passes each found new ones *after* the previous pass appeared
> to have caught them all.
>
> **A pass that finds nothing is evidence of thoroughness, not proof that this
> document is correct.** Re-run the relevant gate, or read the artifact, before acting
> on a line here — including this one. When you verify something, put the date and the
> command in the row.


Single place for what is outstanding, so nothing falls between sessions.
Assembled 2026-09-12; **cleaned 2026-09-16** on `jp/p99-alloc-fixes`. The clean-up:
- **Stale and contradictory fragments removed.** Rows D11, R-j, R-c, R-i, R-h and R-b had accumulated
  notes appended over time that contradicted one another; each row now states its current status
  once.
- **Rows moved to where they belong.** Open items that sat in "Closed" (R-c, R-i, R-h) now sit under
  open work, and D9 (applied in `12613df`) moved to closed.
- **Rows added** for the DL mode, the distance-accuracy evaluation and the AWS runbook.

Nothing measured was dropped. The full history of every item is in `git log` and the reports each row
cites.

**How to read the status column:** *Decision* = waiting on a person, nothing to do until then.
*Staged* = built and verified, not applied. *Blocked* = waiting on access, a machine or time.

---

## 1. Waiting on a decision

| # | item | waiting on | why | file |
|---|---|---|---|---|
| **R6** | **Ground-only variance — closed before this audit's window, with NO surviving artifact.** | **[2026-09-23] Recorded here permanently because it existed in no tracker at all.** Searched the whole tree: the literal token `R6` appears **0 times** in any `.md` or `.py`; `ground-only` appears twice and neither is this item (`docs/research-log.md:541` is a regret-step discussion, `src/gpu/attrition.py:13` is *"height is ground-only"*). **So its result cannot be verified or re-run.** The one piece of good news, checked deliberately: **nothing cites it**, so no untraceable number is in circulation because of it. Status is therefore *unverifiable but harmless* — unless someone later quotes a variance figure whose source they cannot name, in which case this is where it came from. | nobody — recorded, not assigned | `reports/full-project-audit-2026-09-23.md` §2.3 |
| **R10** | **Wide-depression gradient — the only roadmap item with NO work of any kind.** Audited 2026-09-20: **zero commits, zero files, zero mentions** anywhere in the tree, and nothing in this file tracked it, so it could not surface on its own. It was not deferred; it was silently dropped. **UNASSIGNED — it needs an owner or an explicit decision to descope.** Listed here so that whichever happens, it happens on purpose. Sits in the DL + grid-engine lane. | **unassigned — needs an owner, or JP to formally descope** | — |
| **D3** | **R5 — sticky safety-critical class bit.** Which classes (5/6/7 only, or also 1/2)? Which of three decay designs? What is N (frozen `configs/thresholds.yaml`)? Does it affect traversability? | Aakash + three-way | Five sub-decisions; touches frozen `include/vrgrid/cell.py`, so three-way sign-off. | `pending-review/r5-sticky-safety-critical-class-bit.md` §6 |
| **D4** | **R7 README wording** — which of three drafted lengths, and where it goes. | JP | JP said he would place it himself. | `pending-review/r7-readme-counts-draft.md`, `r7-readme-wording.md` |
| **D5** | **`alpha_m2` — raise it or leave it at 0.** | JP | 0 is the only value consistent with sih-math §5.3 and the §5.4 unit test; raising it means restating both in the same commit. **[2026-09-18] Premise re-verified after the merge, and it is intact:** `alpha_m2: 0.0` in `configs/thresholds.yaml:140` and as the dataclass default in `src/grid/splitmerge.py:94`; §5.3's flat-ground remark and §5.4's unit test (c) are true only for α = 0 (Aakash's 28 Aug note says so in the doc); and `test_alpha_would_break_the_flat_ground_remark` (`tests/test_splitmerge.py:427`) passes, so α cannot move quietly. **Also worth knowing before this is treated as a free decision: it is gated on data, not only on judgement.** §5.2 calibrates α against the reference map, and no built reference map exists in the tree — only `scripts/build_reference_map.py`. Until that artifact exists, raising α would be picking a number rather than calibrating one, so "leave at 0" is the only supportable answer today. | `reports/r11-what-this-does-not-do.md` §4 |
| **D6** | **`kappa` 1/16 vs 1/12.** Knowingly 25% low; the stated geometry gives 1/12. | The room | Frozen config, explicitly "the room decides". Both values pinned in tests. | `configs/thresholds.yaml:133` |
| **D7** | **CARLA groundwork — scope.** | JP | Zero existing work; no scope supplied. Starting a simulator integration from a guess is the wrong move. | `MORNING-SUMMARY.md` §3 |
| **D8** | **Machine STATE control for latency, not just an agreed hostname.** | JP + Shrestha | Two honest end-to-end figures exist on two hosts: `README.md` 89.18 / 100.43 ms (Shrestha's i7-14650HX) and this laptop (i7-13620H). **One host also varied 4.4× in one session** (21.01 → 93.44 ms in `ground_cost.py`, with the CPU downclocked to 1520/2400 MHz and ~8 GB over-committed), so agreeing a hostname is not enough. It needs a checked machine state recorded with every figure: CPU clock vs max, commit vs physical RAM, and a warm-up/idle precondition. Controlled A/B ratios survive this; headline absolutes do not. The laptop harnesses now record state per run (`reports/harnesses/machine_state.py`). | `reports/r-b-p99-tail-investigation.md` §11; `docs/handover-2026-09-02.md` |
| **RMSE-BASELINE** | **⚑ Ring-0 RMSE moved 1.7747 → 1.8660 cm at `df35fd5`, the harness gate has been failing since 17 Sep, and nobody noticed** | **[2026-09-23] Cause isolated by measurement, not inference.** Re-running `reports/harnesses/r1_accuracy_by_class.py` on the current tree fails its own built-in gate: ring-0 RMSE **1.8660 cm vs the documented 1.76, 6.0% apart**, 5% tolerance. `r7_hazard_miss.py` shares the gate and stops at the same point — *"M\* is not trustworthy; nothing below would be either."* **Isolation, two experiments:** (1) restoring the old band `[-2.0, 6.0]` leaves it **identical at 1.8660** — the band is ruled out; (2) at **`df35fd5~1`** (verified pre-rework by `bin_points` lacking the vehicle-position argument) it is **1.7747 cm, gate OK**. So **`df35fd5` is the cause** — the D2/R3 ring-boundary rework, which deliberately changed which returns land in which ring (it fixed 0.108% nested footprints and 0.224% of returns being dropped). **The shift is probably an intended consequence of a correct fix that nobody restated.** **Needs a decision, not a re-run:** is 1.8660 the correct new baseline? Accepting it means re-baselining the gate and regenerating both reports; rejecting it means `df35fd5` moved ring-0 accuracy in a way that was not intended. **JP will not re-baseline a gate unilaterally** — that is the judgement this row is waiting on. **[!] This also corrects JP's own earlier claim** that the band invalidated R1/R7; it did not. | **Aakash (his lane and his item) + Shrestha (he made the change)**; JP re-runs once decided | `df35fd5`; `reports/harnesses/r1_accuracy_by_class.py` |
| **BAND** | **⚑ `843ad54` moved the height band under figures measured before it — and the config change was never three-way reviewed** | **[2026-09-23]** `843ad54` (17 Sep, Stxtics03) changed `vertical_extent_m` from **`[-2.0, 6.0]` to `[-3.5, 4.5]`** in **both** `configs/schedule_5_10_20_40.yaml` and `configs/schedule_5_10_50.yaml` — same 8 m span, shifted down 1.5 m — and wired it through `quantise.py`, `metrics.py`, `harness.py`, `kernels.py` and `cuda_kernels.py`. **Process:** `.github/CODEOWNERS` requires **@AakashH2006 @Stxtics03 @victorysingh** on `configs/`, and `CLAUDE.md` says configs are *"frozen before schedules are compared — changing one after that invalidates the ablation"*. The commit is linear with **no PR**, so no review fired. **Technically the change is well-evidenced** — the split was chosen by surveying ground returns beyond 10 m on all eleven sequences, and CPU/GPU came out bit-identical on 200 frames of all eleven plus all 4,071 frames of seq 08 — and **Shrestha did propagate it** into `docs/known-limitations.md` (137 lines) and four presentation docs. **What it did NOT reach:** `reports/r1-accuracy-by-class-and-range-band.md`, `reports/r7-hazard-miss-rate.md`, and **`docs/defense-rehearsal-playbook.md`** (2 Sep — curb-cell counts 4,041 on 07 / 9,499 on 08, ring medians, the 52.3% occupied-set figure), which is **panel-facing**. **[!] CORRECTED 2026-09-23, same day, by measurement:** the first two are stale but **NOT because of the band** — restoring `[-2.0, 6.0]` leaves ring-0 RMSE identical, and the real cause is `df35fd5`. See the **RMSE-BASELINE** row. The playbook's curb and occupancy figures remain **provisionally** band-suspect because they were never re-measured either way — that one is untested, not cleared. Band-INdependent and therefore unaffected: README's memory ratios (21.5×, 286×) and cell counts, FRNet classification accuracy, and latency. | **JP** (his two reports) + **Shrestha** (the playbook and the missing review) | `843ad54`; `reports/full-project-audit-2026-09-23.md` |
| **D12** | **`range_interpolation` — restore nearest-wins, or pin the projection to one thread.** | **Found by Shrestha, `8acbfe9` (20 Sep), after JP flagged that the earlier "it is the model" attribution could not explain a CPU-vs-CPU result.** `src/perception/frnet/frnet.py:156` builds the range image with a duplicate-index assignment — `image[proj_y[order], proj_x[order]] = res[order]` — and leans on last-write-wins so the nearest return claims each pixel. PyTorch documents duplicate-index assignment as **nondeterministic**, and **~35% of a scan's points land on an already-claimed pixel**. Measured (`scripts/frnet_determinism_probe.py`): `threads=16` → image identical **False**; `threads=1` → **True**. Two processes: CPU default threads 0.0729–0.1291% of points disagree, **`torch.set_num_threads(1)` 0.0000%**, CUDA 0.0390–0.0551%. So above one thread the nearest-return rule silently does not hold and the port is not reproducing upstream's projection. **Every frnet-path number was measured in that state** — 86.5% agreement, 90.3% pooled, the by-range table, the fp16 comparison — which Shrestha disclosed rather than buried. **Not fixed, correctly:** pinning to one thread and replacing the write with a deterministic nearest-wins reduction both MOVE published numbers, so it is a decision. **[!] Renumbered by JP 2026-09-23:** Shrestha opened this as **D9**, which was already taken on `jp/p99-alloc-fixes` by the closed `transform_points` allocation item. **My D9 keeps its ID; this becomes D12.** Staged for his copy: `pending-review/d9-collision-renumber-to-d12.md`. **[!] The file is in JP's lane** (`src/perception/`), so the fix is JP's to make once the decision is taken. | **JP (the code is in his lane); the decision is JP + Shrestha** | `8acbfe9`, `scripts/frnet_determinism_probe.py` |
| **D10** | **ROS 2 adapter scope** — window extent, bitfield as one float layer or six boolean ones, where live labels come from, publish rate, and whether `export_gridmap()` needs arguments. | Three-way | The last changes a **frozen signature**. **The blocker is throughput:** `query()` is 54.7 µs, so a 20 m window at 5 cm is 8.75 s per frame (0.11 Hz), and a vectorised bulk reader pinned bit-identical to `query_region` is a prerequisite. **Method decided 2026-09-13:** transcribe `query()` rather than re-derive it. **Step 1 (transcription) done as prep.** Only `_refined` is new work, and `query()` contains no reduction (no ULP risk). Step 2 not started. Two traps are **named checks** (§6a): `test_bulk_query_keeps_the_two_outside_paths_independent` (the two OUTSIDE paths diverge on 361 of 361 swept points when a window is un-tracked; `OUTSIDE == -1`, so a conflated mask silently reads the last ring) and `test_bulk_query_masks_before_gathering`. | `pending-review/ros2-adapter-design.md` §4, §7; `pending-review/query-vectorisation-transcription.md` |
| **R-c** | **stdout UTF-8 at script entry points — apply or not.** | JP | **Staged, not applied** (`d0d84c3`). JP chose an in-code entry-point fix over `PYTHONUTF8=1`; `pending-review/r-c-stdout-utf8-entry-points.diff` covers 20 scripts. Verified: UTF-8 output byte-identical on 18/18 executed scripts, all 12 cp1252 `--help` crashes fixed, script tests 31 passed / 1 skipped on the patched tree. **[2026-09-18] Evidence now complete: 20/20 executed** — `frnet_fast_scatter` and `gen_demo_rrds` were run patched and unpatched (identical; the only differing lines are `frnet_fast_scatter`'s own benchmark timings). **New gap from the merge:** the diff covers 20 of 27 `scripts/` entry points; of the seven it misses, `bench_cupy_seam.py` and `vram_contention.py` crash under cp1252 — both GPU-lane, Shrestha's. Does not cover `python -m vrgrid.run`, harnesses, or `open()` defaults. | `pending-review/r-c-stdout-utf8-entry-points.md` |
| **R-i** | **Unify the synthetic and real timing-table printers — or not.** | JP | **Decision doc, nothing implemented** (`d0d84c3`). The split caused both the 80.78 ms and 8.15 → 1.31 MB mislabellings. Recommendation: unify the printers, keep `run()` / `run_real()` separate, snapshot-test both current tables first, and update `whole_frame_bench.py`'s row regex in the same commit. **[2026-09-18] The snapshot step is DONE and committed separately** — `tests/test_timing_table_printers.py`, 8 tests, hand-fed timings so the tables are identical on every machine. It pins both printers' columns, both total labels (`MEASURED` vs `FRAME`), the lower-bound caveat, the `bin`-allocates-a-MB regression warning, and the inclusive `p99 <= 100 ms` budget boundary. **The decision itself is untouched and still JP's**; the tests make either answer safe, and one of them is written to fail loudly if the printers are unified. | `pending-review/r-i-unify-timing-table-printers.md` |
| **PUSH** | **Push `jp/p99-alloc-fixes` and open a PR.** | JP | 38+ local commits, never pushed; no upstream. Also decides how code reaches the AWS instance (the runbook defaults to a git bundle). | — |

---

## 2. ~~[!] The one that blocks other work~~ — CLOSED 2026-09-23

> **[2026-09-23] This section was stale and is corrected here rather than deleted.** It claimed, in the present tense, that D1 was *currently distorting measurements other decisions depend on*. **It is not, and has not been since the merge `ef4524e`.** `3c26d47` (AakashH2006) added a per-run `ground.reset_estimator()`; `test_real_sequence_replay_is_identical` **passes**, and `tests/test_determinism.py` is **13/13** (re-run 2026-09-23, immediately before this edit). D1 itself was **DECIDED as option A** and the convention is now enforced by `tests/test_ground_reset_convention.py` (5 tests, AST-based, verified non-vacuous). The row has moved to §5.
>
> **Two further corrections to the bullets below.** The claim that *"every harness written this week resets the estimator … (`ground._estimator = None`)"* is out of date: the four plain pokes were migrated to the public `reset_estimator()` in `eeedcda`, leaving nine private assignments that all install a *configured* estimator. And the **3.60 cm** figure in the third bullet is itself **provisionally invalidated** by the 17 Sep band change — see the **BAND** row and the banner on `reports/r1-accuracy-by-class-and-range-band.md`.
>
> **What is NOT closed:** the trap itself. Any new entry point that calls `segment_ground*` and forgets the reset re-creates the defect; that is what the enforcement test exists to catch, and it caught one on its first run (`reports/harnesses/ground_cost.py`, now R-k).

**Kept for the record — what the defect looked like while it was live:**

- *(historical)* `test_real_sequence_replay_is_identical` fails, correctly. Two replays in one process differ by
  1,245 of 1,479,013 points. Resetting the estimator between replays makes them identical (verified
  2026-09-14).
- **[2026-09-18 — SUPERSEDED by `ef4524e`, which merged the fix in; the gate has passed here since. Kept verbatim as the record.]** It still fails **in this clone**, which does not contain the upstream point-fix (`3c26d47`, AakashH2006, verified present on `vrgrid26/main` after a fetch). That fix is a per-run `reset_estimator()` at the entry points — a call-site convention, not the lifetime design this row is about.
- **~18% of the published seq-07 ring-1 RMSE is artifact**, not coarsening error (3.60 → **3.04 cm**
  once the reference and the map use a consistent ground mask).
- **No ring-1 accuracy comparison finer than ~0.5 cm can be adjudicated until it is fixed.**

The fix direction is measured, not speculative: two estimators making one pass each agree exactly, with
0 of 1.48 M points differing. Every harness written this week resets the estimator before each pass
(`ground._estimator = None`) without editing `ground.py`.

---

## 3. In progress, staged, or blocked on access / people

| # | item | status | waiting on | evidence |
|---|---|---|---|---|
| **AWS** | **The DL pipeline's real-time figure, and GPU reproducibility** | **Blocked / paused by JP (2026-09-14).** **[2026-09-20] SUPERSEDED — AWS was never used and this row's premise is void.** `docs/research-log.md` records AWS as *abandoned, not deferred*: every GPU quota read 0, the increase was refused (the free plan has no GPU tier), **lifetime spend $0.00**, bucket and guardrails torn down. **The whole T4 pass ran on Kaggle's free T4**, and the hardware is recorded in `docs/gpu-lane/t4/host.log` as **2× Tesla T4, 15,360 MiB, driver 580.159.04**. So there is nothing to activate, nothing to spend and nothing to cancel. `reports/aws-dl-realtime-addendum.md` now carries a superseded banner; `docs/gpu-lane/02-AWS-RUNBOOK.md`, `11-AWS-RESUME.md` and `scripts/aws/` still read as live plans and need the same note from their owner. **What this does NOT settle:** the T4 figures carry no machine in the documents that quote them — see `reports/number-provenance-audit-2026-09-20.md`. **[2026-09-18] Owner corrected twice.** First: the CUDA/GPU *port* is Shrestha's, not JP's. Then JP refined it — **AWS itself is shared and he can obtain credentials**, so this row is not blocked on Shrestha alone; what JP lacks is a **CUDA device on his own machine**. Upstream now scripts this end to end — `docs/gpu-lane/02-AWS-RUNBOOK.md`, `scripts/aws/t4.sh`, `auto.sh` — and the blocker is the **account**, answering `OptInRequired` for EC2 and `NotSignedUp` for S3, which only its owner can clear. What remains JP's here is narrow: the DL-mode measurement protocol (`reports/aws-dl-realtime-addendum.md`) and the outward-facing question of whether `jp/p99-alloc-fixes` gets pushed so the instance can reach it. **Our runbook was demoted to that addendum on 2026-09-18** and no longer duplicates upstream's launch/transfer steps. Needs an AWS g4dn.xlarge; no local CUDA build by decision, and this laptop has no AWS access. **Runbook revised 2026-09-16 after a pre-launch audit:** git-bundle transfer (the branch is unpushed), frames 0–200 (494 MB), explicit CUDA torch + `.[perception]`, a Patchwork++ check, an on-instance 2-frame preflight, Linux-correct state rules, results fetched before termination, and a termination checklist. The laptop-testable parts were tested. | **JP or Shrestha — AWS is shared and JP can supply credentials.** Shrestha owns the GPU port itself; JP owns the DL-mode measurement protocol and the push decision. What is *not* available to JP is **local** CUDA hardware, so anything needing a card must run on the instance, never on his laptop | `reports/aws-dl-realtime-addendum.md` |
| **D11** | **FRNet: checkpoint, verification, DL mode** | **Checkpoint acquired, verified, DL mode built; real-time GPU number still pending.** See the breakdown below. | the AWS run | `reports/d11-frnet-reverification-r2.md` |
| **GPU-LATENCY** | **⚑ Two GPU frame-time figures disagree, and they must always be quoted together** | **[2026-09-20] Both are real; neither alone is the answer.** **(a) The committed artifact that MISSES the budget:** `docs/gpu-lane/t4/timing_cuda.log` — seq 08, 200 frames, schedule **`5/10/20/40`**, `--device cuda`, on Kaggle's **2× Tesla T4** (`docs/gpu-lane/t4/host.log`) → **p50 67.42 / p99 100.18 ms, max 479.51 — "MISSES 10 Hz at p99"**. This is the only committed CUDA timing log in the repo and **no document cites it**. **(b) The figure that MEETS the budget but has no artifact:** **p50 21.94 / p99 28.40 ms, 3.5× headroom**, schedule **`5/10/50`** — quoted in `OPEN-ITEMS`, `docs/gpu-lane/13-PS-SCHEDULE.md` and `docs/research-log.md`, and present in **no `.log` or `.json`**. The same is true of the **10 Hz DL claim, p50 71.8 / p99 79.5 ms** (`--semantics frnet --semantics-precision fp16`), which additionally **names no machine anywhere, including its own commit `d48831a`**. **The honest sentence is both at once:** *on the schedule the project has always published (`5/10/20/40`) the T4 misses 10 Hz at p99 by 0.18 ms, and on the problem statement's own schedule (`5/10/50`, which drops a ring) it meets it with headroom — and only the first of those has a committed log.* Quoting either alone is misleading. **Needed:** re-run the `5/10/50` and DL passes with `--frame-times` and commit the artifact, naming the host the way `06`/`07`/`09`/`10` already do. | Shrestha (his lane and his runs) | `docs/gpu-lane/t4/timing_cuda.log`, `docs/gpu-lane/t4/host.log`, `reports/number-provenance-audit-2026-09-20.md` |
| **R-j (cont.)** | **⚑ Upstream attributes the same non-determinism to a different cause** | **[2026-09-20]** `d48831a` and `docs/research-log.md` report FRNet disagreeing with itself on **0.028–0.046% of points across two CPU runs** and conclude *"it is the model"*, **without stating a thread count**. R-j measured the opposite cause on this branch: at PyTorch's default intra-op thread count neither `--fast-scatter` nor the port's loops reproduce per point, and with **`torch.set_num_threads(1)` both are exact and identical**. The two are consistent only if upstream ran at the default — in which case the cause is the **thread pool**, not the model. Same class of error as the attribution withdrawn in `1a47960`. **Not corrected in upstream's files — flagged for its owner.** | Shrestha (his conclusion), JP (evidence) | `reports/number-provenance-audit-2026-09-20.md` §D |
| **R-j** | **`--fast-scatter` reproducibility** | **CPU: RESOLVED. GPU: OPEN, not assumed.** See the breakdown below. | the AWS run (steps e, f) | `reports/bench/rj_round2_pred_diff.json` |
| **R-h** | **`cleanup` / `ground` tail** | **`cleanup`:** the LUT guard (`d540618`) and the `_centres` fast path (`697a2bd`) are applied on the branch and **await Shrestha's review** (`engine.py` is his). Instrumenting every line showed `_centres` (7.09 MB, 10.78 ms p50) was the stage's largest allocator, with `np.isin` (6.40 MB) second; an earlier claim that `np.isin` set the peak was corrected in `dce017a`. After both fixes no single cleanup line spikes on slow frames, and `visibility_cleanup`'s ~11.5 ms is float compute, not allocation (not attempted: ULP risk). **`ground`:** in isolation Patchwork++ has p99−p50 ~1 ms, against 11.13 ms in-pipeline in an earlier measurement; in the later clean-machine benches its p99−p50 was ~1.4–2 ms at baseline and after. **Answered 2026-09-16 from existing trusted data, no new timing needed:** every whole-frame bench recorded a per-stage `ground` row with machine state checked. In-pipeline `ground` p99−p50 is a median **1.40 ms before any fix** (3 runs) and **2.46 ms after all fixes** (10 runs), max 7.67 ms. The 11.13 ms never reproduces on a trusted run, so the allocation hypothesis is **not supported** (the tail was already small before the fixes). The 11.13 ms came from a pre-gating probe with no state record: machine state is the likely, but unproven, cause. Rare slow frames (26–33 ms against p50 ~19.5) remain unattributed. `reports/r-b-p99-tail-investigation.md` §13, `reports/harnesses/ground_inpipeline_spread.py`. | Shrestha (review of the `cleanup` changes) | `pending-review/cleanup-isin-guard.md`; `reports/r-b-p99-tail-investigation.md` |
| **UPSTREAM** | **`vrgrid26/main` has moved a long way past this branch** | **Fetched 2026-09-18 (read-only, JP authorised): `7dee296`, 59 commits we do not have.** Shrestha ported the frame loop and then the whole pipeline to the card (`03abd7b`, `6af6907`; upstream reports 22 ms/frame, bit-identical to CPU), closed D2 (`df35fd5`), fixed two regret-costmap bugs, rebalanced the 8 m band, and scripted the AWS T4 lane (`scripts/aws/t4.sh`, `auto.sh`); Aakash rebuilt the dashboard and fixed the estimator sharing (`3c26d47`). **Dry-run merge (disposable worktree, nothing kept):** conflicts in `src/run/__main__.py` (6 hunks, in `iter_pipeline`/`perceive`/`main`), `scripts/timing_table.py` (3), `dashboard/__main__.py` (2), `OPEN-ITEMS.md` (2), `README.md` (1). **`src/run/engine.py` auto-merges** — upstream did not touch `_cleanup` or `_centres`, which are byte-identical to our base. A clean auto-merge is not proof of correctness; the merged file must be run. **Consequences:** our AWS runbook is superseded by theirs; R-b's pooled p99 89.58 ms describes the pre-port CPU path and needs restating, not retracting; the DL-mode wiring must be re-expressed against upstream's new `perceive()`. | **DONE — merged `ef4524e`** | `reports/pre-merge-collision-check.md`, `reports/merge-upstream-verification.md` |
| **README** | **One README / report writing pass** | Waits for the AWS numbers, so every caveat lands together: motion from ground truth, the 50 m label ceiling, the real-time figure's hardware and GPU reproducibility, R-j's thread-count condition, and the opt-in DL mode. README still describes a ground-truth-label-only pipeline. | JP, after AWS | — |

**D11 — breakdown.** Offline evaluation plus an opt-in pipeline mode. The live default stays ground truth.
- **Checkpoint:** the 4 Sep file is gone (Shrestha doesn't have it; never tracked). The authors' public
  release was downloaded 2026-09-14T07:27:54Z, SHA-256 `09adea90…285e`, corroborated by Google's crc32c
  and by an independent browser download. It is **not** the 4 Sep file. `eff969a`, `8e20d57`.
- **Accuracy (Step 4):** 90.3% point accuracy / 65.2% mIoU (15 classes) / 61.1% drivable on seq 08,
  200 frames, matching the recorded figures. Fine-tuning was skipped: the figures are the pretrained
  checkpoint's. `4675e95`.
- **Plan-regret delta (Step 6):** reproducible at `--fast-scatter --threads 1`, two bit-identical runs.
  FRNet labels raise plan regret from 1.160 to 2.230 (**+1.069**) longitudinal and from 1.142 to 1.710
  (**+0.567**) lateral. The ground-truth arm is identical to the passed oracle control. This supersedes the
  10-thread +0.997 / +0.449. `5c19b96`. **Paired per query (2026-09-16):** longitudinal +1.069, 95% CI [+0.730, +1.436], 49/1/14 worse/equal/better, sign p=1.1e-05; lateral +0.567, 95% CI [+0.301, +0.846], 25/31/8, sign p=0.0046. Both CIs exclude zero. The 10-thread lateral value lies inside the CI, so the earlier sensitivity is bounded. The CI covers the 64 queries on one map and one slice, not other scenes.
- **Opt-in DL mode:** `--semantics frnet` in the pipeline, dashboard and timing table; motion stays
  ground truth. Its labels equal `frnet_eval.py`'s model on a real frame. `a26f7f0`.
- **Accuracy by distance:** `--threads 1`, reproducible. 93.2% (0–10 m) / 88.1% (10–25 m) / 85.4%
  (25–50 m). SemanticKITTI seq 08 has no labels beyond 50.0 m. `a86b491`.
- **Still pending:** the DL pipeline's real-time figure on GPU (AWS).

**R-j — breakdown.**
- **CPU, resolved (`b20e03e`):** the run-to-run variance is PyTorch's intra-op thread pool, on both
  paths. With `torch.set_num_threads(1)`, `--fast-scatter` equals the port's loops on every point and
  repeats exactly (0 of 2,471,164 points differ across four passes). At the default 10 threads
  neither path repeats: 1,895 points (fast) and 1,446 points (loop) differ between runs, with headline
  metrics unchanged. The reproducible like-for-like speedup is about 7.0× (1 thread, 20 frames:
  129 s vs 898–914 s). The earlier 5.82× (10 threads) is only a wall-time ratio.
- **GPU, open:** CUDA atomic-add ordering and cuDNN algorithm choice are a different mechanism, so the
  CPU answer is not carried over. Harnesses are ready (`988c3d8`): `frnet_pred_diff.py --device cuda`
  and `shim_determinism_probe.py --device cuda`; runbook steps (e) and (f). CPU reference hashes:
  scatter_max `51503245c7f03b0d`, scatter_mean `393d23dd065277e6`.
- **Rule until GPU is measured:** quote `--fast-scatter` as reproducible only at 1 thread on CPU.
  `a86b491`'s distance table was run that way (`threads: 1` in its JSON), so no R-j caveat applies to
  it.

---

## 4. Ready to pick up — scoped, no decision needed

| # | item | notes |
|---|---|---|
| **R-d** | **`ruff` E741 in `tests/test_metrics.py:472`** (ambiguous `l`). Pre-existing on `origin/main` from `014d388`, still red. | One-line fix; not assigned (tests/ is outside this lane). |

---

## 5. Closed — recorded so nobody re-raises them

| item | outcome | commit |
|---|---|---|
| **R-b** — the p99 tail | **Gate met on this laptop.** Pooled p99 **89.58 ms** over 1,000 frames (5 fresh-process runs, all state-trusted), p50 79.74 ms, against a baseline of 101.30 / 122.96. Five bit-identical fixes: transform scratch `12613df`, cleanup LUT `d540618`, reflectivity skips the unused incidence field `35b7b27`, `_centres` fast path `697a2bd`, `project()` winner selection `b9eef67`. **Limits:** 3 of 1,000 frames still exceeded 100 ms; one machine (D8); the `engine.py` fixes await review (R-h); D1 unchanged. **[2026-09-18] RESTATED after the merge `ef4524e` — the figure stands, its scope narrowed, and it is NOT retracted.** Two qualifiers now apply and both belong with the number wherever it is quoted: (1) it is the **CPU host path before Shrestha's GPU port**; upstream now runs the pipeline on the card and reports 22 ms/frame, a different execution model on different hardware, so the two are not comparable and neither supersedes the other. (2) it is the **pre-`df35fd5` grid**: the merge's host-parity run shows map output diverging from frame 0 in `binned` (123,254 -> 123,389), `out_of_view`, `protected`, `cells_touched`, `occupied` and `tested`, because of upstream's ring-boundary and ring-0 rho work. The timings were not re-measured after the merge, and this machine was paging when the parity run was taken, so **no post-merge p99 exists on this branch**. The honest sentence is "pooled p99 89.58 ms on the pre-port CPU path against the pre-`df35fd5` grid, one laptop" — not "the pipeline's p99 is 89.58 ms". **[2026-09-20] ⚑ Upstream's competing closure has no artifact.** Upstream closed R-b at **p50 21.94 / p99 28.40 ms, 3.5× headroom** (`5/10/50`, `--device cuda`). Traced: those two numbers appear in **prose only** — `OPEN-ITEMS.md`, `docs/gpu-lane/13-PS-SCHEDULE.md`, `docs/research-log.md` — and in **no committed `.log` or `.json`**. Meanwhile the one CUDA timing artifact that IS committed, `docs/gpu-lane/t4/timing_cuda.log`, records **p50 67.42 / p99 100.18 ms — "MISSES 10 Hz at p99"** on `5/10/20/40`, and **no document mentions that result**. Both can be true at once (different schedules, and `5/10/50` drops a ring), but the repo currently shows the favourable number without its raw log and hides the unfavourable one inside a log nothing cites. Neither supersedes the CPU figures below. Detail: `reports/number-provenance-audit-2026-09-20.md`. **[2026-09-18] RE-MEASURED ON THE MERGED TREE, and the merge did not regress latency:** pooled p99 **87.69 ms** (95% CI 86.85–89.26), p50 81.19, 1 frame of 1,000 over 100 ms — 5 fresh-process runs on a warm, settled machine, state OK before and after each. **[!] New finding: a pass whose FIRST run starts from a cold page cache MISSES the gate** — pooled p99 **101.70 ms**, 19 frames of 1,000 over 100 ms. Both passes of 1,000 frames are reported and nothing was discarded; the pre-merge 89.58 ms happened to run warm, which is why this was never visible. Whether the gate is defined on a warm machine is now itself an open question. Full denominators and the percentile-method cross-check in `reports/r-b-post-merge-p99.md`. | `36a4dbe`, restated `ef4524e`, re-measured post-merge; `reports/merge-upstream-verification.md` §1, `reports/r-b-post-merge-p99.md` |
| **D9** — `transform_points` allocation | **Applied** with JP's approval: an opt-in `reuse_buffers` scratch, so callers that retain frames keep the allocating path. Bit-identical, and transform p99 fell from 22.07 to 1.45 ms. | `12613df` |
| **D11 checkpoint provenance** | Public release, SHA-256 `09adea90…285e`, recorded before first use and independently corroborated. | `eff969a`, `8e20d57` |
| **Opt-in DL mode** (SIH26053 "deep-learning pipeline") | `--semantics frnet` in `python -m vrgrid.run`, `python -m vrgrid.dash` and `timing_table.py --seq`; ground truth stays the default, motion ground truth (disclosed). Pipeline FRNet labels equal `frnet_eval.py`'s on a real frame. Suite 728 passed / 1 failed (D1) / 3 skipped. Smoke-tested again 2026-09-16 (2 frames, exit 0). | `a26f7f0` |
| **FRNet accuracy by distance** (SIH26053 "Performance Metrics") | 93.2 / 88.1 / 85.4% point accuracy at 0–10 / 10–25 / 25–50 m, with drivable / static / movable group IoUs; bands reproduce the whole-slice 90.3036% / 65.18%. **No ground truth beyond 50.0 m** in seq 08 (measured). A tally bug was caught by a unit test before the real run. | `a86b491` |
| **AWS runbook + GPU latency harness** | Written, then revised after the 2026-09-16 audit (see §3 AWS). | `5925f6b`, `988c3d8`, this clean-up's commit |
| `num_iter=2` latency tradeoff | **Rejected on measurement.** Buys 5.42 ms, costs 19–54% of ring-0 RMSE concentrated on slopes. | `c0ee403`, `860d58c` |
| The 80.78 / 97.72 ms latency claim | **Retired** across all seven files that carried it; reproduces to 1.1% only as a synthetic back-half benchmark. | `5cddaa7` |
| 69.8% mIoU | **Corrected to 65.2%** in the four files that carried it. | `c632027`, `f3a0337` |
| `pypatchworkpp` unpinned | **Pinned to ==1.4.1**, also the fastest published wheel. | `23648e9` |
| `r9` self-contradiction | Withdrawn in place. | `23648e9` |
| `VRGRID_DATA_ROOT` unset failing as "missing data" | **Fixed:** names the cause and the fix. | `fac61c2` |
| Ring-1 reproduction mismatch (seq 07) | **Root-caused:** the singleton (D1). | `870766c` |
| Build provenance (native build vs wheel) | **Closed as a documented unknown** on JP's call. | — |
| R11 limits page | **Done.** | `e4bd731` |
| **D1** — Patchwork++ estimator lifetime | **DECIDED 2026-09-23 as option A** (keep the module-level estimator; reset once per run at entry points). `src/perception/ground.py` is **unchanged** — A requires no code change to it. The weakness A was chosen with, that nothing enforced the convention, is now covered by `tests/test_ground_reset_convention.py`: 5 tests, AST-based so a **new** entry point is caught rather than a listed one, verified non-vacuous by planting an offender and watching it fail by name. It also pins that `iter_pipeline` resets **once per run, not per frame** — per-frame would stay deterministic and silently add a Patchwork++ construction to the timed path. Four plain `ground._estimator = None` pokes migrated to the public call; **nine private assignments remain**, all installing a *configured* estimator, which is the cost A knowingly accepts and the evidence that would justify revisiting B or C. Options doc kept: `pending-review/d1-patchworkpp-estimator-lifetime.md`. | `eeedcda`, `68eb5fc`; gate 13/13 re-verified 2026-09-23 |
| **D2 / R3+R4** — ring boundary under anisotropy | **CLOSED upstream by `df35fd5`** (17 Sep, Stxtics03), *"close Aakash's open items"*. **Verified here 2026-09-20 by reading the diff, not the message:** ring membership is now decided per world-lattice BLOCK coarse to fine (`lattice.ring_of`, `ring_of_into`, the CUDA bin kernel); `bin_points` takes world points plus vehicle position and heading. The defect was live at v = 0 on real data — seq 08, 30 frames: nested footprints on every frame (0.108% of coarse cells) and 0.224% of returns dropped, both 0 now. **R4's CI test landed with it** as `tests/test_lattice.py::test_no_cell_footprint_contains_another_under_foveation` (parametrised by schedule), alongside `test_every_return_inside_the_map_is_binned`. Done by Shrestha while Aakash was away, with every change to that lane listed in `docs/handover-2026-09-17-aakash.md` for review. **[!] This row sat in §1 "waiting on a decision" with Aakash as owner for three days after it was closed** — the same staleness upstream flagged on D1, pointing the other way. | `df35fd5`; `docs/handover-2026-09-17-aakash.md` |
| **R-k** — `ground_cost.py` never resets the estimator | **Found 2026-09-18 by `tests/test_ground_reset_convention.py` on its first run; both halves now measured, and the published figures survive.** The harness warms up and then benches `reps=3` over the same scans with no reset, so reps 2 and 3 start from state rep 1 left behind. **Correctness half:** agreement 96.5% fresh vs 96.5% carried-over (+0.07 points) — R-a's published 96.5% stands — though the masks *do* move: 10 of 20 frames, **5,878 of 2,471,164 points (0.2379%)**, reproduced to the digit in a second process. **Timing half** (machine state OK before *and* after, 3 fresh processes per configuration): mean of rep medians **19.26 ms** as published (19.16 / 19.31 / 19.32) vs **19.40 ms** resetting per rep (19.28 / 19.51 / 19.42) — **+0.14 ms, 0.7%, inside the published configuration's own 0.16 ms spread.** So the carry-over is real but immaterial to both the agreement figure and the latency, and **R-a's 21.01 ms needs no restatement on account of the reset.** [!] Separate observation, logged not folded in: today's tree and machine give **~19.3 ms against the 21.01 ms of record** (2026-09-13, pre-merge) — an 8% gap well inside D8's known envelope for this very harness, which once varied 4.4x on one host. It is a machine/day difference, not a consequence of the reset, and it is why D8 remains open. `ground_cost.py` itself was deliberately left unmodified, its output being published provenance. | `1884358`, plus this commit; harnesses `ground_estimator_carryover.py`, `ground_cost_reset.py` |
| **[2026-09-23] Re-verification sweep — 4 previously unchecked closed rows** | **All four hold. No new false closure found in this pass.** `pypatchworkpp` unpinned → **pinned**, `pyproject.toml:33` `pypatchworkpp==1.4.1`. `VRGRID_DATA_ROOT` unset failing as "missing data" → **fixed**, an unset variable now reports *"$VRGRID_DATA_ROOT is NOT SET, so this fell back to data"* rather than pretending the data is absent. The retired **80.78 / 97.72** claim → appears only as a retraction (`docs/presentation/00-START-HERE.md:88` names it as *"the back half only"*), never as a live figure. **`num_iter=2` tradeoff** → consistent: `num_iter` appears nowhere in `src/` or `configs/`, only in harnesses, so it was closed as an investigation and never applied to shipped code — `pending-review/patchworkpp-num-iter-tradeoff.md` remains the proposal. **Caveat, per the standing note above: this pass finding nothing is evidence, not proof.** | — | this row |
| **R-a** — commit the measurement harnesses | **Done**, each with a PROVENANCE header. | `2955b14`, `8318fed` |
| **R-f** — seq 00 ring-1 residual | **Closed:** the singleton carrying state across sequences. | `reports/ring1-reproduction-investigation.md` §7 |
| **R-e** — 99.87% vs 99.5% | **Resolved by recomputation:** 99.87% dead cells; README was right. | `3a43c19` |
| "Zero allocation in the frame loop" | **Scoped, not retracted:** back end only (the front end allocates ~39.5 MB/frame on seq 08), and the CI test measures retained growth, not churn. `--alloc` now fails loudly with `--seq`. | `235986d` |
| **R-g** — extend the invariant to perception | **Done:** `test_no_retained_growth_in_the_perception_frame_loop`, asserting retained growth only. | `8b40e44` |
| **DL** — `--fast-scatter` re-verification (11 Sep) | Done, exact in both directions on CPU. Superseded in scope by R-j. | `ff774fe` |
| ROS adapter scoping | **Done as design only.** | `0035850` |
| Ring 0 has no ρ (`known-limitations` §2b) | **FIXED 2026-09-17.** Within-cell variance in `spread`; ring 0 ρ 1.17 [1.13–1.29], n = 11 (after the §11 band fix). Roughness in the regret costmap keeps between-cell variance only. | — |
| §9.2 per-ring reference restricted to what each ring received (Aakash handover 2 Sep) | **DONE 2026-09-17.** `RingObservations`; `known-limitations` §9. | — |
| §2b eleven-sequence table stale | **REGENERATED 2026-09-17.** Ring 1 ρ 1.39 [1.22–1.53] (was 1.45 [1.26–1.59]); deck docs updated. | — |
| Money plot non-monotone step (Aakash handover 2 Sep) | **RESOLVED 2026-09-17** for seq 08 — paired over the same queries it is noise. Two real steps found elsewhere, see §5b. | — |
| `python -m vrgrid.dash` CPU-only; stale GPU note in `dashboard/gpu_stats.py` | **FIXED 2026-09-17.** `--device cuda`. | — |
| **D2 / R3** ring boundary under anisotropy | **FIXED 2026-09-17** (Shrestha, Aakash away). Ring membership decided per world-lattice block, coarse to fine; partition now CI-tested at adversarial speeds and headings. The defect was live at v = 0 on real seq 08 (nesting every frame, 0.224% of returns dropped); both 0 after. Snapping (part B) not used — see the status block in the file. | see `git log -- src/grid/lattice.py` |

*(rows above this line from `jp/p99-alloc-fixes`; the six below and §5b are upstream's,
merged 2026-09-18 from `vrgrid26/main` @ `7dee296`.)*

---

## 5b. New open items, 2026-09-17 — found while closing Aakash's list

| # | item | notes |
|---|---|---|
| ~~**N-1**~~ | ~~Ring 3 on 08 / 09 / 10~~ **FIXED 2026-09-17.** Out-of-band heights were fused and re-basing hid saturation. 08 ρ 1.84 → 1.03. | `known-limitations` §11 |
| ~~**N-5**~~ | ~~Band floor ~2–3 m below road level~~ **REBALANCED 2026-09-17** to −3.5 / +4.5 m (still 8 m), chosen by survey of all eleven sequences. Seq 04 ring 3 24.5 → 18.2 cm; every ring 3 within 4% of its own returns. | `known-limitations` §11 |
| ~~**N-2**~~ | ~~Seq 07 uniform 20 cm regret spike~~ **FIXED 2026-09-17.** Footprint heights were count-weighted, which leaked neighbouring cells into planning cells; now area-weighted like the reference. Seq 07 monotone. | `known-limitations` §10 |
| ~~**N-6**~~ | ~~Seq 09 uniform 40 cm < 20 cm regret~~ **FIXED 2026-09-17.** M*'s class came from the first ground return and the map's from all returns; M* now takes the majority of all static returns. No uniform curve has a backward step past 2 SE on any sequence. | `known-limitations` §10 |
| **N-3** | **Seq 00 ring 2**: ρ 2.20 → 1.02 against its own returns. | Cross-look disagreement at 20–50 m, not coarsening. Cause open. `known-limitations` §9. |
| **N-4** | GitHub #6 ("dashboard/ has been removed") is stale since PR #7 brought Rerun back. | Shrestha's own issue; close it when convenient. |

---

## 6. The pattern worth acting on

Twice in two sessions a published figure turned out to be **unverifiable because its harness was
session scratch** (the p50 80.78 / p99 97.72 ms latency, and the seq 00 ring-1 RMSE 6.77 cm). In both
cases the number was probably fine; **the missing artifact was the defect.** Every measurement since has
committed its harness and its JSON beside the result (R-a).

---

## 7. Housekeeping state (2026-09-16)

- **Branch:** `jp/p99-alloc-fixes`, 38+ commits ahead of `main`, **never pushed**, no upstream. `main`
  still holds the local vrgrid-26 merge, also not pushed.
- **Untouched:** `src/perception/frnet/` (frozen), `src/perception/ground.py` (D1 paused),
  `src/grid/lattice.py`, and `include/vrgrid/`.
- **Modified on the branch, awaiting review:** `src/run/engine.py` (`d540618`, `697a2bd`), Shrestha's
  file.
- **Gates** (last full run, at `a26f7f0`): `pytest` 728 passed / 1 failed (the D1 determinism test) / 3
  skipped. `ruff` 1 error (R-d, pre-existing).
- **Checkpoint:** `checkpoints/frnet-semantickitti_seg.pth` present locally, **not tracked**, SHA-256
  re-verified 2026-09-16. `checkpoints/frnet-semantickitti_seg.pth.PROVENANCE.md` is tracked.
- **`pending-review/` inventory:**
  - **Applied:** `handover-latency-line-correction`, `r7b-mIoU-69.8-is-an-error`,
    `timing-table-unicode-crash`; `cleanup-isin-guard` and `transform-points-allocation` on this branch
    (their stale "nothing applied" headers were corrected in this clean-up).
  - **Withdrawn:** `patchworkpp-num-iter-tradeoff`.
  - **Superseded:** `stdout-utf8-at-entry` (by R-c), `front-end-allocation-instrumentation` (by R-i).
  - **Waiting:** D2 `r3-…`, D3 `r5-…`, D4 `r7-readme-*`, D10 `ros2-adapter-design` +
    `query-vectorisation-transcription`, R-c, R-i.
- **Summaries:** `MORNING-SUMMARY-3.md` (R-c / R-i pass), `-4.md` (D11 and R-j), `-5.md` (SIH26053
  fit).
