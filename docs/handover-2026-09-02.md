# Handover, 2 September — who does what next

46 commits today. Everything below is on `main` and pushed. This is what is
left, split by whose lane it is, with the decisions separated from the work.

The dataset is complete: **43,552 scans, 84.8 GB, all 22 sequences**, labels on
00–10, poses on all. `python scripts/data_status.py` re-verifies it and exits 0
when whole.

---

## What is proven, and what is not

**Measured on real data and holding:**

| claim | figure | where |
|---|---|---|
| accuracy | ρ **1.45** [1.26–1.59] over **11 sequences**, ring 1 | `eval_synthetic.py --seq` |
| memory | **29.06 MB** vs uniform 10 cm's **78.50 MB** at matched extent | `memory_bound.py`, `regret_plot.py` |
| latency | seq 08 frame **p50 108.65 ms**, **p99 127.23** — **does not meet 10 Hz on this host**, see note | `timing_table.py --seq 08 --frames 200` |
| ghost removal | **13.5%** of trail removed, 4.96 M cells cleared | `ghost_removal_figure.py --seq 08` |
| curbs | ring 0 **8.1–9.1 cm** on all 11 sequences | `feature_report.py --seq` |
| segmentation | **90.3%** point accuracy, **65.2%** mIoU (paper 73.3%) | `frnet_eval.py --frames 200`, seq 08 |
| fine-tuning | tested and **rejected**, −0.5 mIoU — see Shrestha item 3 | `frnet_eval.py --checkpoint` |

**Latency note — read before quoting the row above.** Measured 2026-09-12 on
`main` @ `fac61c2`: 200 frames of seq 08, schedule `5/10/20/40`, **whole frame**
(perception *and* map — the `FRAME` row, not a back-end subtotal). Real
Patchwork++ confirmed active *by banner on the run*: no `semantic-class
FALLBACK` warning, `PatchWorkpp::PatchWorkpp() - INITIALIZATION COMPLETE`
present. Host: Intel i7-13620H (laptop, 16 logical), 15.7 GB, Windows 11,
Python 3.13.9, numpy 2.4.4, `pypatchworkpp` 1.4.1 (PyPI wheel). CPU-only —
`src/gpu/` is kernel-shaped numpy, not CUDA.

The figure is **host-sensitive**: the `ground` stage alone costs 20-21 ms here
against **12.41 ms** on Shrestha's machine (`docs/research-log.md`, 4 Sep) — a
difference large enough to change the verdict on its own. And **p99 is
duration-dependent**, ranging 115-147 ms across windows of a single 221-frame
run, so a p99 quoted without a frame count and a host is not a checkable number.

### [!] Two latency figures now coexist in this repository. Both are honest.

The vrgrid-26 merge brought in a second end-to-end measurement, and they
disagree:

| where | figure | host |
|---|---|---|
| `README.md` (and `scripts/timing_table.py`'s docstring) | **89.18 ms p50 / 100.43 p99**, max 109.28 | Shrestha's machine, `docs/research-log.md` 4 Sep |
| the row above | **108.65 ms p50 / 127.23 p99** | Intel i7-13620H laptop, spec'd above |

Both hosts are identified: `README.md:288-289` states that every latency figure
there was measured single-threaded on an **Intel i7-14650HX**, which is the
machine behind 89.18 / 100.43. The row above is an **i7-13620H**. *(Correction:
an earlier version of this note suggested there might be a third host. That was
a misreading — README:288-289 identifies the second host rather than introducing
a third. D8 is a two-host problem.)*

**Neither is wrong and neither supersedes the other.** They are the same
quantity — whole-frame, 200 frames of real seq 08, real Patchwork++ — measured
on different machines. The gap is consistent with the `ground`-stage difference
already documented above (20-21 ms here vs 12.41 ms there), and it is large
enough to flip the 10 Hz verdict: 100.43 p99 is 0.43 ms *over* budget, 127.23 is
27 ms over.

**This is open item D8** (`OPEN-ITEMS.md`) — *one agreed reference host and one
agreed command* — and it needs a decision from JP and Shrestha together. **It
was deliberately not resolved during the merge**, because picking one number
would have meant discarding a real measurement, and averaging them would have
invented a third that nobody ran. Quote whichever you quote **with its host
attached**, and do not present either as "the" frame latency until D8 is called.

> **RETIRED: the previous figure of p50 80.78 ms / p99 97.72 ms, "meets 10 Hz".**
> It was cited to `timing_table.py --seq 08`, which does not produce it —
> that command gives 105-112 ms p50 on every run at every frame count. The
> figure reproduces to **1.1%** as `timing_table.py --cells 910000`: the
> **synthetic** path with candidate cells raised to the schedule's full
> allocated slot count. It is a real measurement of a different thing — a
> mislabelled synthetic benchmark result, not a seq-08 measurement.
>
> The original run is **unrecoverable** — no log, no artifact, no script, and no
> method recorded in commit `464ad8b` — so what was actually run cannot be
> confirmed. The reproduction is strong circumstantial evidence and corroborates
> Shrestha's independent 4 Sep note calling the figure *"the back half on a
> synthetic sweep"*. Full archaeology in
> `reports/latency-gap-investigation.md`.

**Not proven, and must not be claimed:**

- **10 Hz.** Not established on any agreed reference machine. The whole frame
  is 108.65 ms p50 / 127.23 p99 on the host above, and no configuration
  measured reaches the 100 ms budget at p99 — including with the ground stage
  cut by a third (`reports/numiter-tradeoff-accuracy-cost.md`, rejected on
  accuracy grounds). p50 is within reach; **p99 is not, and its cause has not
  been located.**
- **The plan-regret figure.** R(S) is comparable across schedules at a fixed
  window and *not* across windows. On the longitudinal query the frozen
  schedules lose to uniform; on the lateral query they rank 1st, 1st, 2nd, 5th,
  2nd across five runs. There is no knee.
- **Potholes.** 56–551 cells per sequence, 10× spread, no pattern. A
  demonstration, not a rate.
- **Ring 0 ρ.** There is none, on any sequence. Diagnosed 3 Sep as a metric
  arithmetic gap, not a data or sensor limit — fixable, deferred to Day 7. See
  item 4 in Shrestha's list.

---

## Shrestha — decisions only you can make

**1. Sign off four changes to a `frozen: true` file.** `configs/thresholds.yaml`
says *"if these move between runs you are comparing tuning effort, not
schedules."*

| key | change | reads it |
|---|---|---|
| `traversability.baseline_m` | added, `0.50` | §7.1 bits 1–2, eq. (22a) |
| `features:` | whole block added | §7.4 only |
| `visibility.max_candidate_cells` | `150000` → `null` | §10.4 scratch |
| `features.curb.baseline_m` | tried `0.30`, **reverted** to `0.20` | §7.4 |

Only `baseline_m` changes a predicate. The others add keys or size working
memory. The curb one is already reverted — eleven sequences said 0.30 tripled
the cross-sequence spread to buy 0.7 cm of median.

**2. The visibility cap trade.** `null` = the grid's slot count = 910,000 =
**58.24 MB**, truncation impossible by construction. An explicit `600000` =
**38.40 MB**, covers every measured sequence at 1.32× the observed peak
(314,442 / 455,714 / 278,226 on 07 / 08 / 00). I took the safe side because the
failure is silent; the cheaper side is defensible. One line either way.

**3. The fine-tuned checkpoint — MEASURED, and the answer is don't keep it.**
600 steps, decode head, class weights 3× on terrain and vegetation, trained on
00–07/09/10 with an assert keeping sequence 08 out. Scored on the same 200
frames of 08 by the same script:

| class | base | tuned | Δ | |
|---|---|---|---|---|
| road | 97.4 | 97.3 | −0.1 | guard |
| sidewalk | 89.2 | 88.8 | −0.4 | guard |
| **terrain** | 73.8 | **74.7** | **+0.9** | target |
| **vegetation** | 70.8 | 70.7 | −0.1 | target |
| trunk | 79.3 | 75.1 | **−4.2** | |
| building | 89.1 | 87.5 | −1.6 | |
| bicycle | 47.9 | 46.3 | −1.6 | |
| fence | 29.4 | 28.2 | −1.2 | |
| **mIoU (15)** | **65.2** | **64.7** | **−0.5** | |
| drivable (5) | 61.1 | 61.4 | +0.3 | |
| point accuracy | 90.3 | 90.3 | 0.0 | |

Terrain moved, everything else paid for it, and the gain on the five classes
the map consults is +0.3 — inside noise. The checkpoint was already converged
on this data and there was no headroom to buy, exactly as the per-class support
table predicted before we started.

**Keep `frnet-semantickitti_seg.pth` as the reported model.** Keep
`frnet-finetuned-terrain.pth` on disk with this table beside it: a documented
negative result is a stronger defense position than never having tried, and it
is one slide.

⚑ The training loss fell 0.167 → 0.1425 across the run. That is exactly why
  training loss is not a result — a class-weighted loss falls when the head
  gets more confident on the weighted classes, whether or not it gets more
  correct. Sequence 08 was the only honest read.

**4. Ring 0 has no ρ on any sequence — DIAGNOSED 3 Sep, and the reason above
was wrong.** This item previously read "excludes footprints holding a single
reference *return*, and at 5 cm essentially all of them do." Both halves are
false. `block_stats` counts observed **cells**, not returns, and a ring-0
footprint is `k = 1` — exactly one cell — so `n_ref` can never exceed 1 and
`coarsening_ratio_per_ring`'s `n_ref > 1` guard drops ring 0 **by arithmetic,
on every sequence**. The data is there: M\* holds more than one return in
**92.8 / 97.0 / 97.4%** of the ring-0 cells §9.2 scores on 00 / 07 / 08 (median
6–12 returns), and a single HDL-64E sweep already puts >1 return into 57–66% of
the 5 cm columns inside 10 m. M\* sums those returns but discards the sum of
squares, so the within-cell spread that would give ring 0 a ρ is computed at
build time and thrown away.

Closing it would score ring 0 at **ρ 1.01–1.24 — the best of any ring on all
three sequences.** It is deferred rather than done because `block_stats` is
shared: the same change moves ring 1 by **−21.5%** on 07 and −7.5% on 08 (rings
2–3 under 2.3%), invalidates every cached M\* `.npz`, forces a regeneration of
the eleven-sequence table and the "ρ median 1.45" headline, and reaches
`plan_regret.costmap_from_reference`. Every ρ moves in the direction that
flatters us.

**So: a fixable gap with a measured cost, deliberately deferred to Day 7 — not
a permanent limitation.** Full diagnosis, tables and the narrower-variant
trade-off in `docs/known-limitations.md` §2b.

---

## Aakash — `src/grid`, `src/eval`

**Review what I changed in your lane today.** All committed, all tested, none
of it discussed with you first:

- `metrics._compared` — now requires `height_variance > 0` (a cell whose
  returns were all non-ground holds ground_height 0, which is *the datum*, so
  the metric's answer moved with the datum: 3.19 → 6.15 cm from a datum shift
  alone) and excludes band-saturated cells.
- `metrics` — added `mean_bias_cm` and `above_frac` beside `bias_cm`, which is
  RMS and cannot distinguish "systematically high" from "randomly scattered".
- `reference_map.block_class()` — new; M\* had no class opinion, so §7.1 bit 4
  was charged on one side of eq. (23) only.
- `reference_map._Builder.add` — takes a ground mask; it was accumulating every
  static return, so a building façade landed in the same 5 cm column as the road.
- `plan_regret.common_support` — `require_confident=True` by default.
- `eval_synthetic.plan_regret_for` — averages over **64** queries. One query was
  never an estimator; the same schedule read 2.207 / 0.207 / 0.000 / 0.414 at
  20 / 40 / 80 / 160 frames.

**The real work: §9.2's per-ring comparison.** Each ring is scored against a
reference containing observations that ring never received. An outer cell stops
updating once the vehicle approaches and it migrates inward, so ring 2 holds
only far-range data while M\* holds everything. A metric comparing each ring
against a reference *restricted to what that ring actually observed* would
isolate coarsening properly. This is the item standing between the project and
a defensible §8.2.

**Two open numbers.** Sequence 00's ρ of 2.32 at ring 2 (every other sequence
1.18–1.76) — its pose bias is fixed and that moved ρ only 2.40 → 2.32, so the
rest is dispersion and is unexplained. And the money plot's remaining
non-monotone step, which survives both the extent fix and 64-query averaging.

---

## JP — `src/perception`, `dashboard`

**1. Review the pose-source change.** `loader.POSE_SOURCE_BY_SEQUENCE` is now
`{"00": "slam", "08": "slam"}`. Sequence 08's official GT poses disagree between
consecutive frames by **16.6 cm** consistently; SLAM poses give 1.04. Sequence
00 accumulates **−13.95 cm** of bias by ring 3 on GT and −1.18 on SLAM. All
eleven sequences are tabulated in `test_only_08_needs_the_slam_poses`. Note
per-frame agreement turned out to be a *weak predictor* — 00 reads 2.27 cm/frame
and 03 reads 1.97, and their accumulated bias differs by 17×.

**2. The FRNet port works.** ~15% → **98.3%** point accuracy *on seq 00
frame 43*, and **90.3%** point accuracy / **65.2%** mIoU *over 200 frames of
seq 08* against the paper's 73.3%. (Those are two different runs and were
previously quoted as one; the mIoU was also recorded as 69.8%, which is an
arithmetic error — see `docs/research-log.md` 4 Sep.)

Three fixes. **Divergences 2 and 3 were the two the
old header described wrongly; divergence 1 it got right** — corrected in the
port's own header and in `perception-dashboard-summary.md` §3 on 3 Sep:

- **1, right as written.** activation `nn.LeakyReLU` → `nn.Hardswish` at 7
  sites in `frnet_backbone.py` (mmcv HSwish is `x·relu6(x+3)/6`, which *is*
  `nn.Hardswish`);
- **2, wrong place.** The FOV bug was **not** in `frnet.py`, which always
  defaulted correctly to 3.0 / −25.0 — `semantics.py` was overriding it with
  the HDL-64E's *physical* FOV (2.0 / −24.8) out of `configs/frnet.yaml`. Two
  different quantities: the checkpoint learned a FIXED projection. Now pinned
  as `FRNET_TRAIN_FOV_*` constants a sensor config cannot reach;
- **3, understated.** `range_interpolation` was called "a missing
  densification (H=64, W=2048)", which reads as a resolution setting. It is a
  *test-pipeline transform to reproduce verbatim*, and it projects with the
  divergence-2 FOV — the two were coupled. Transcribed from upstream including
  an off-by-one (`proj_idx > 0`, so point index 0 reads invalid against a −1
  sentinel) reproduced **deliberately**, because the paper's 73.3% was measured
  with it and "fixing" it makes our numbers incomparable.

Two further factual corrections to that header: it said "413/413 tensors, no
shape mismatch" where the checkpoint loads **421, 0 missing and 8 unexpected**
(all `auxiliary_head.*`, training-only, correctly unused at inference); and it
named `frustum_encoder.py`'s manual `scatter_max`/`scatter_mean` as a suspected
fourth cause — they were not one, and that file is unchanged.

This is your module. It is reported alongside the map and never swapped into
it — semantics still come from the `.label` files so the mapping contribution
stays isolated.

**3. Patchwork++ is installed but not reproducibly.** `pip install
pypatchworkpp` fails at **every** published version: the sdist's CMake fetches
`.../tags/v${CMAKE_PROJECT_VERSION}.tar.gz`, the variable is empty under
scikit-build-core, GitHub 404s. Building from a git clone works because it
takes the `if(EXISTS ../cpp/)` branch:

```
git clone --depth 1 https://github.com/url-kaist/patchwork-plusplus.git
.venv/bin/pip install ./patchwork-plusplus/python
```

That belongs in the setup docs — without it `real_scans` silently falls back to
`ground_from_semantics`, which includes `terrain` and admits embankments.

**4. The dashboard does not show today's work.** Curbs, potholes and per-cell
confidence all exist (`grid/features.py`, `grid/confidence.py`) and nothing
renders them. If the demo is meant to show what the map knows, that is the gap.

---

## If time runs short

Cut in this order: the DL fine-tune (the pretrained model already satisfies the
requirement), the §9.2 metric restriction (document it as known instead), the
dashboard work. Do **not** cut the threshold sign-off or the visibility cap
decision — both change what shipped numbers mean.
